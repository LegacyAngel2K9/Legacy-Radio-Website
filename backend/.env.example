# Server configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5001
API_BASE_URL=http://localhost:5000/api

# Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=password
DB_NAME=legacy_radio_db

DB_STORAGE=./database.sqlite

# JWT Secret
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h
JWT_COOKIE_EXPIRES=30

#  Stripe Payment Gateway
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_signing_secret
STRIPE_CURRENCY=usd

# PayPal Payment Gateway (if implementing)
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_SECRET=your_paypal_secret
PAYPAL_ENVIRONMENT=sandbox # or 'live' for production

# Email Configuration (for notifications)
EMAIL_HOST=smtp.your-email-provider.com
EMAIL_PORT=587
EMAIL_USERNAME=your-email@example.com
EMAIL_PASSWORD=your-email-password
EMAIL_FROM=no-reply@yourdomain.com

# Subscription Settings
BASE_SUBSCRIPTION_PRICE=10 # $10/month
DEFAULT_SUBSCRIPTION_DAYS_GRACE_PERIOD=7 # Grace period after expiration

# Admin Security
ADMIN_ACCESS_KEY=your_admin_access_key

# CORS Configuration
CORS_ORIGIN=http://localhost:3001